services:
  opengist:
    image: ghcr.io/thomiceli/opengist:1.12
    container_name: opengist-test
    restart: unless-stopped
    ports:
      - "6157:6157"
      - "2222:2222"
    volumes:
      - ./opengist-data:/opengist
    environment:
      UID: 1000
      GID: 1000
